kind = "web"
previewPath = "/"
title = "McSpencer Enterprise"
version = "1.0.0"
id = "artifacts/mcspencer"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 3000

[services.development]
run = "pnpm --filter @workspace/mcspencer run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/mcspencer", "run", "build" ]
publicDir = "artifacts/mcspencer/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "3000"
BASE_PATH = "/"
